# El panel de administración de Django (django.contrib.admin) está
# desactivado en esta primera etapa porque requiere base de datos.
# Cuando se conecten los modelos (ver models.py), este archivo se usará
# para registrar Colaborador, Horario, Asistencia, Falta, Incidencia y
# Amonestacion con admin.site.register(...).
